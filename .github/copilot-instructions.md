## Running Instructions
- You should always activate the virtual environment before running any scripts:
  - On Windows: `.\.venv\Scripts\activate`
  - On macOS/Linux: `source .venv/bin/activate`
- You can then use `python` or `python3` to run scripts within the activated environment.